const mutations = {
    setTagline(state, tagline) {
        state.tagline = tagline;
    },
    setEthos(state, ethos) {
        state.ethos = ethos;
    },
    setCoreValues(state, coreValues) {
        state.coreValues = coreValues
    }
};

export default mutations;
