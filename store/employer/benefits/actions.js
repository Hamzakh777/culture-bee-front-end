const actions = {
    addBenefit({ commit }, id) {
        try {
            console.log('removing update');
        } catch (error) {
            alert(error);
        }
    },
    deleteBenefit({ commit }, id) {
        try {
            console.log('removing update');
        } catch (error) {
            alert(error);
        }
    }
}

export default actions;